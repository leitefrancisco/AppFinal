<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppFinal.Views.Feed"
             xmlns:vm="clr-namespace:AppFinal.ViewModels"
             Title= "{Binding Title}">
    <ContentPage.BindingContext>
        <vm:FeedViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="2*" />
                <RowDefinition Height=".5*" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Image Source="indie" BackgroundColor="White"
                   Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" />
            <Editor Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1"
                    Placeholder="Save Your Achievements" FontSize="Medium"/>
            <Button Grid.Row="2" Grid.Column="0" Text="Save" />
            <Button Grid.Row="2" Grid.Column="1" Text="Erase" />

            <Label Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Text="All Saved!"
                   FontSize="Large"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>